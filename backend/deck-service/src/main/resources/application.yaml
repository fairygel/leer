server:
  port: 8082

spring:
  application:
    name: deck-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/deck
    username: postgres
    password: postgres
